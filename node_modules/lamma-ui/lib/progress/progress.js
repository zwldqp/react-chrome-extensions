'use strict';

exports.__esModule = true;
exports.default = Progress;

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _propTypes = require('prop-types');

var _propTypes2 = _interopRequireDefault(_propTypes);

var _rcProgress = require('rc-progress');

require('rc-progress/assets/index.css');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * @author sunyue
 *
 * 基于[rc-progress](http://react-component.github.io/progress/)
 */
function Progress(props) {
    if (props.type === 'line') {
        return _react2.default.createElement(_rcProgress.Line, props);
    }
    if (props.type === 'circle') {
        return _react2.default.createElement(_rcProgress.Circle, props);
    }
}

Progress.propTypes = {
    /**
     * 请在这里填写组件当前属性的描述信息
     */
    type: _propTypes2.default.oneOf(['line', 'circle'])
};

Progress.defaultProps = {
    type: 'line'
};