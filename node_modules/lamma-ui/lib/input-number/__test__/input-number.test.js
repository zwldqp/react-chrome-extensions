'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _enzyme = require('enzyme');

var _inputNumber = require('../input-number');

var _inputNumber2 = _interopRequireDefault(_inputNumber);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('Test <InputNumber/> component basic render', function () {
    var testFunc = jest.fn();
    var props = {
        min: 1,
        max: 10,
        defaultValue: 3,
        onChange: testFunc
    };
    var inputNumber = (0, _enzyme.shallow)(_react2.default.createElement(_inputNumber2.default, props));
    it('should render without throwing an error', function () {
        expect(inputNumber.find('input').exists());
        inputNumber.simulate('change');
        expect(testFunc).toHaveBeenCalled();
    });
}); /* global describe, it, expect */