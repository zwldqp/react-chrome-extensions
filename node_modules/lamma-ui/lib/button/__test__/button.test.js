'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _enzyme = require('enzyme');

var _button = require('../button');

var _button2 = _interopRequireDefault(_button);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('Test <Button/> component basic render', function () {
    var testFunc = jest.fn();
    var props = {
        onClick: testFunc
    };
    var button = (0, _enzyme.shallow)(_react2.default.createElement(
        _button2.default,
        props,
        '\u6211\u662F Button'
    ));
    it('should render without throwing an error', function () {
        expect(button.find('button').exists());
        expect(button.find('button').text()).toEqual('我是 Button');
        button.simulate('click');
        expect(testFunc).toHaveBeenCalled();
    });
}); /**
    * @author sunyue
    */

/* global describe, it, expect */