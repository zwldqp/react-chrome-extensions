'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _enzyme = require('enzyme');

var _switch = require('../switch');

var _switch2 = _interopRequireDefault(_switch);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('Test <Switch/> component basic render', function () {
    var testFunc = jest.fn();
    var props = {
        size: 'small',
        onChange: testFunc
    };
    var rcswitch = (0, _enzyme.shallow)(_react2.default.createElement(_switch2.default, props));
    it('should render without throwing an error', function () {
        expect(rcswitch.find('span').exists());
        rcswitch.simulate('change');
        expect(testFunc).toHaveBeenCalled();
    });
}); /* global describe, it, expect */